FarCall allows to call external batch files and import environment variables to Far process.

how to use:

1. type call:batch\_file\_name
or
2. set cursor on batch file, press F11 & select "Far Call" plugin