- видимая перерисовка при быстром поиске
- с быстрого поиска не переходит по Tab на другую панель
- быстый поиск прерывается по Ctrl
? проблемы с переносом по словам
- просмотр хтмл-писем в редакторе
- один раз выпал при просмотре ящика с quickview.
- проблемы в диалоге вопроса о перезаписи.

--------------------------------------------------------------------------------

AY>>> П.С. А что ожидает Mailbox_Open*? NULL или INVALID_HANDLE_VALUE?
DT>> NULL
AY>>> А покажет ли оно эрор в таком случае?
DT>> а надо? и если надо, то зачем.
AY> Не смогли открыть мэйлбокс, например POP3 недоступен или папка для
AY> Folder не существует.
это не совсем правильно. думаю, надо что-то типа:
    enum
    {
      errorSuccess = 0,
      errorStd1,
      errorStd2,
      ...
      errorStdN,
      errorUser   = 0x1000,
      errorFailed = -1
    };
    /**
     * @param fileName mailbox file name
     * @param createNew tell how to open mailbox
     * @param result pointer to opened mailbox
     * @return standard (errorXXX) or user (errorUser+1) error code
     */
    DWORD Mailbox_Open( LPCSTR fileName, BOOL createNew, LPHANDLE * result );
    /**
     * @param error code (always greater than errorUser)
     * @return text of user error message
     */
    LPCSTR Mailbox_GetErrMsg( DWORD error );

--------------------------------------------------------------------------------

AY> А почему не работает Alt+РусБуква? Может лучше сделать свою более умную
AY> обработку Alt+Letter?
а  это  и так полностью мою "умная" функция, ее пришлось писать потому
что  фар умеет искать только по имени файла, а у меня сабжи могут быть
любой длинны... надо править.

--------------------------------------------------------------------------------

МВ не распознаёт кодировку. Если
кодировка по умолчанию KOI-8 то и письмо по F3 будет в KOI-8 хотя оно в
Windows-1251 и charset выставлен.

DT: мультисекционное письмо с кодировкой в текстовой секции

--------------------------------------------------------------------------------

AY>> 2. Смотрю бокс (mbx), захожу в письмо (enter), выхожу, курсор
AY>>    позиционируется на первом файле а не на котором были.
DT> какой режим сортировки, режим тридов? это во всех ящиках?
Любой, любой, во всех. В бета 8 всё ОК. Уточнение, позиционируется не на
первом а на "..".


--------------------------------------------------------------------------------
добавить multipart/related